mixin script(src)
  script(src=src)

mixin style(src)
  link(rel='stylesheet', href=src)

mixin balance(amount)
  - if (amount >= 0)
    span.balance.owed= '$' + amount
  - else
    span.balance.you_owe= amount.toString().replace('-', '-$')